In /Users/syyui/projects/OpenHilbert.jl Intotally rewrite the entire reposity. Please read OpenHilbert.jl and rewrite the entire reposity OpenHilbertBenchmark.jl.

You should delete methods that OpenHilbert.jl has deleted and change the input parameters as the functions API change in OpenHilbert.jl.

Revise OpenHilbertBenchmark.jl until all tests pass.

**You also need revis test case, including delete some case and change API**